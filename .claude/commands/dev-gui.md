Launch the GUI in dev mode with hot-reload.

Ensure shared + CLI are built first, then start Vite + Electron dev.

Run:
```bash
npm run build -w @proteus-forge/shared && npm run build -w @proteus-forge/cli && npm run dev -w @proteus-forge/gui
```
