Run tests and coverage across all packages.

Run:
```bash
npm run test -w @proteus-forge/shared && npm run test -w @proteus-forge/cli && npm run test -w @proteus-forge/gui
```

Report per-package coverage vs 80% threshold.
